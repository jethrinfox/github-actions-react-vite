name: "Main Pipeline"

on:
    push:
        branches:
            - "main"

jobs:
    build:
        runs-on: ubuntu-18.04
        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-node@v3
              with:
                  node-version: 14.18.1
            - run: yarn install
            - run: yarn build
